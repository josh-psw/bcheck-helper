package ui.view.pane.storefront.bcheck;

import ui.view.pane.storefront.ActionController;

class BCheckPopupMenu extends StorefrontPopupMenu {

    BCheckPopupMenu(ActionController actionController) {
        super(actionController, "BCheck");
    }
}
